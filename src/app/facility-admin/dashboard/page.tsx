import Link from "next/link"; import { dataService } from "@/services/data-service"; import { Title } from "@/features/admin/components/admin-ui"; export default function Dashboard(){const q=dataService.inquiries().filter(x=>x.organizationId==="01");return <section><Title title="Selamat datang, Admin Fasyankes" subtitle="Ringkasan RS Harapan Kota dalam mode demo."/><div style={{display:"grid",gridTemplateColumns:"repeat(4,minmax(0,1fr))",gap:14}}>{[["Status verifikasi","Verified"],["Inquiry baru",q.filter(x=>x.status==="New").length],["Perlu ditanggapi",q.filter(x=>x.status!=="Closed").length],["Kelengkapan profil","92%"]].map(([label,value])=><div className="card" style={{padding:18}} key={label as string}><span className="hint">{label}</span><b style={{display:"block",fontSize:25,color:"var(--navy)",marginTop:9}}>{value}</b></div>)}</div><div className="card" style={{padding:22,marginTop:20}}><h2 style={{marginTop:0,fontSize:19,color:"var(--navy)"}}>Feedback terbaru</h2><p>Kesulitan menghubungi mitra — nomor telepon belum dapat dihubungi pada sore hari.</p><Link href="/facility-admin/profile" className="btn btn-primary">Perbarui profil</Link></div></section>}
