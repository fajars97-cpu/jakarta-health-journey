import { PublicShell } from "@/components/layouts/public-shell"; import { PartnerCard } from "@/features/catalog/components/partner-card"; import { dataService } from "@/services/data-service"; import { PartnerType } from "@/types/domain"; import { Info } from "lucide-react";
export default function Support(){const list=dataService.listPublic().filter(x=>![PartnerType.Hospital,PartnerType.Clinic].includes(x.type));return <PublicShell><main className="container" style={{padding:"50px 0"}}><p style={{color:"var(--teal)",fontWeight:800}}>DUKUNGAN PERJALANAN</p><h1 className="section-title">Dukungan opsional untuk perjalanan Anda.</h1><div className="card" style={{margin:"24px 0",padding:16,display:"flex",gap:10,background:"#f1f8fb"}}><Info color="#1261a6"/><span><b>Dukungan opsional.</b> Mitra tidak memberikan rekomendasi atau keputusan medis.</span></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(250px,1fr))",gap:16}}>{list.map(x=><PartnerCard key={x.id} partner={x}/>)}</div></main></PublicShell>}
